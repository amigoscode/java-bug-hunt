# Hint -- Bug 91

How does Java compare boxed `Long` objects with `==`? Is there a range of values where this happens to work, and a range where it does not? Check the `Long.valueOf()` cache behavior.
