# Hint -- Bug 97

Does `ElectricVehicle` have one `speed` field or two? What happens when a subclass declares a field with the same name as a parent's field? Which field does the parent's method see?
