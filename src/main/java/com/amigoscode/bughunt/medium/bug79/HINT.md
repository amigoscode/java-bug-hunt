# Hint -- Bug 79

There are two ways to check if a regex appears somewhere in a string: `Matcher.matches()` and `Matcher.find()`. One requires the entire string to match. Which one does this code use, and is that correct for a "contains at least one" check?
