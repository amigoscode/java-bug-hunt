# Hint -- Bug 93

What happens when you subtract `Integer.MIN_VALUE` from `Integer.MAX_VALUE`? Can the result fit in an `int`? Think about what `(a, b) -> a - b` does when `a` and `b` are very far apart.
