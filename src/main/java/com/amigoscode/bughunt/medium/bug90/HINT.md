# Hint -- Bug 90

How do you correctly check whether a `double` value is `NaN` in Java? Try evaluating `Double.NaN == Double.NaN` and `Double.NaN != Double.NaN` -- the results may surprise you. Is there a utility method on `Double` for this?
