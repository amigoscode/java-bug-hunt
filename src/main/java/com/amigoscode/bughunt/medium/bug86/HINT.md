# Hint -- Bug 86

What kind of `List` does `Collections.singletonList(...)` return? Try calling `add()` or `remove()` on it and see what happens. Is there another way to initialise a mutable list that starts with one element?
