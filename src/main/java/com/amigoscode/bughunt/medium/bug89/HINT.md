# Hint -- Bug 89

Does `HashMap` guarantee that entries are iterated in the order they were inserted? Try adding several entries to a `HashMap` and printing them out. Is there a `Map` implementation that preserves insertion order?
