# Hint -- Bug 67

What happens when you call `LocalDate.plusMonths(1)` on January 31? Now call `plusMonths(1)` again on that result. Is the day-of-month the same as if you had called `startDate.plusMonths(2)` from the original date?
