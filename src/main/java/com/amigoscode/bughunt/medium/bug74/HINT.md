# Hint -- Bug 74

Look at the terminal operation used after `peek()`. Is it guaranteed to consume every element of the stream? Check the Javadoc for `Stream.peek` -- what does it say about short-circuiting operations?
