# Hint — Bug 62

Look at the `catch` block in `processPayment`. How many different exception types can be thrown by `validate`, `checkFraud`, and `charge`? Does the catch block distinguish between them?
