# Hint -- Bug 85

What does `String.join` do when one of the elements in the array is `null`? Does it skip it, leave an empty gap, or substitute something unexpected? Try it with `String.join(",", "a", null, "c")` and check the output.
