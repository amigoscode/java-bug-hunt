# Hint -- Bug 99

Can static methods be overridden in Java? What happens when a subclass defines a static method with the same signature as the parent? How are static method calls resolved -- at compile time or runtime?
