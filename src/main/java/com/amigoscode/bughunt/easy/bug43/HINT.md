# Hint — Bug 43

Trace `depth()` on a two-level tree: root with one child. `child.depth()` returns 1. Then what does root's recursive call do with it?

Does the current directory count toward the depth?
