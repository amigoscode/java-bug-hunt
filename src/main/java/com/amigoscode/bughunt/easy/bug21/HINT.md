# Hint — Bug 21

The test with small values passes. The test with large values fails. Why would comparison behave differently based on the *magnitude* of the numbers?

Look at the types — what is `a` and `b`? What does `==` mean for those types?
