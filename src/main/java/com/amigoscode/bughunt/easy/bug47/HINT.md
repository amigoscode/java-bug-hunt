# Hint — Bug 47

Trace `reverseInPlace(new int[]{1, 2, 3, 4})` by hand. How many times does the loop run? How many swaps happen?

If you swap position 0 with 3, then later swap 3 with 0 — what's the net effect?
